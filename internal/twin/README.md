# Twin Domain Package

This package is the complete dependency-free Go domain implementation. Records
must be finite, unique by day, and chronological before modeling. Public output
uses an explicit field allowlist and is non-medical.

See [`docs/API.md`](../../docs/API.md) and the generated
[`go-symbols.md`](../../docs/generated/go-symbols.md).
