# Commands

`flyto2/` contains the single local CLI entrypoint. It dispatches imports,
generation, prediction, evaluation, public export, registries, reports,
benchmarks, equipment gates, workflow metadata, privacy checks, and the bounded
toy simulation. See [`docs/CLI.md`](../docs/CLI.md).
