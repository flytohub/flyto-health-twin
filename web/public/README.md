# Public Build Inputs

Only static assets and explicitly redacted `public-data.json` belong here.
`robots.txt` defaults to `Disallow` and is replaced with an indexable policy only
for a verified production URL. Never add raw wearable exports, credentials,
locations, private health values, or full reports.
