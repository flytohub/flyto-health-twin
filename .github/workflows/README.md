# GitHub Workflows

- `ci.yml` installs pinned toolchains and golangci-lint, then runs repository
  lint, complete verification, demo, and Flyto2 Indexer strict audit.
- `documentation.yml` reuses the organization documentation contract.
- Other checked-in workflows own repository security automation.
