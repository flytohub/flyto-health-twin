# Repository Automation

Workflows run the complete source, documentation, privacy, build, and security
contracts on `main` and pull requests. Reusable organization workflows remain
read-only unless a job explicitly requires artifact or security-event writes.
